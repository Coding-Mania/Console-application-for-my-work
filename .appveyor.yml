version: 1.0.{build}
branches:
  only:
  - master
  - development
  - ^version-.*$
image: Visual Studio 2019
configuration: Release
clone_depth: 1
before_build:
- cmd: dotnet restore
build:
  project: GoldJewelry.sln
  verbosity: minimal
